import HomeLab from "@/components/home/HomeLab";

/**
 * Home. All content and motion live in HomeLab (client component);
 * metadata and the Event JSON-LD come from the root layout.
 */
export default function Home() {
  return <HomeLab />;
}
